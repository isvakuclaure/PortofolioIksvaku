/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // Ajustar si el repositorio está en un subdirectorio
  // basePath: '/Iksvaku_Portfolio',
};

export default nextConfig; 